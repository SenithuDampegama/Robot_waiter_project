waiter_orchestrator:
  ros__parameters:
    # timeouts (seconds)
    order_timeout_s: 60.0
    nav_timeout_s: 45.0
    kitchen_timeout_s: 60.0
    deliver_timeout_s: 30.0
    return_timeout_s: 45.0
    retry_limit: 2

    # topics (mock-friendly)
    "topics.orders": "/orders"
    "topics.order_events": "/order_events"
    "topics.table_pose": "/table_pose"
    "topics.nav_status": "/nav_status"
    "topics.kitchen_status": "/kitchen_status"
    "topics.mission_state": "/mission_state"
    "topics.goal_pose": "/goal_pose"
